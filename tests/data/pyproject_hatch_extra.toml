[project]
name = "check-dependencies"

[project.optional-dependencies]
test_optional = ["test_extra"]

[tool.hatch.envs.default]
dependencies = [
    "test_main",
    "test_1",
    "test_extra > 0",
]

[tool.hatch.envs.dev]
dependencies = [
    "test_dev_1",
]

[tool.hatch.envs.test]
dependencies = [
    "test_dev_2",
]

